version: 2

models:
  - name: DEMO
    description: "dbt_code"
    columns:
      - name: "EMPID"
        data_tests:
          - unique
          - not null
          - accepted _values:
              arguments:
                values: ["1", "2", "3", "4"]
                config:
                  severity: warn

sources:
  - name: EMP
    database: DBT_DB
    schema: PUBLIC
    tables:
      - name: EMPLOYEE_RAW
